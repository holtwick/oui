@require "../../stylus/index.styl";

.oui-slider {
  position: relative;

  ._slider {
    position: absolute;
    transition: all 250ms ease-out;
    pointer-events: none;
    z-index: 1;

    &._no-animate {
      transition: none;
    }

    &._hidden {
      display: none;
    }
  }
}